<!DOCTYPE html>
<html lang="en">
  <%- include('../partials/head.ejs')%>
<body>
    <%- include('../partials/nav.ejs')%>
   

<!--content-->
<div id="index-banner" class="parallax-container">
  <div class="section no-pad-bot">
    <div class="container texth1">
      <br><br>
      <h4 class="header center  white-text  customfont">Charitable ❤🧡💛</h4>
    </div> 
  </div>
  <div class="parallax"><img src="/img/Screenshot (44).png" alt="image" width="100%" height="20%" ></div>
</div>

    <div class="container">
        <h5 class="light black-text" style="font-size: 19px; font-family: 'Arial Narrow', Arial, sans-serif;">Charitable is a website create to help old and needy poeple in the society.
         This project was collected by a group of University students. This website has an interrupt user interface, it is secured, it is also instable meaning it can be installed 
         on an android or an Iphone or any device and most of all its fun to use once you have signed-in. Click the link below to check the website.
        </h5>
        <h5 class="light customfont"><a href="https://charity-5a2c8.web.app/" target="_blank" >Link to Charitable</a></h5>
    </div>
     <br><br><br><br><br>
    
     <script type="module">
      import{signout,onAuthStateChanged,auth}from '/fireConfig.js';
  //logout
  const logout=document.querySelector('.logout');
  logout.addEventListener('click',()=>{
   signout().then(
          window.location.href='/'
        );
  })
  const logout2=document.querySelector('.logout2');
      logout2.addEventListener('click',()=>{
        signout().then(
          window.location.href='/'
        );
      })
  
  //ui authentication
  const loggedinLink=document.querySelectorAll('#logged-in')
  const loggedoutLink=document.querySelectorAll('#logged-out')
  
  const setupUI=(user)=>{
   if(user){
     loggedinLink.forEach(item=>item.style.display='block')
     loggedoutLink.forEach(item=>item.style.display='none')
   }else{
     loggedinLink.forEach(item=>item.style.display='none')
     loggedoutLink.forEach(item=>item.style.display='block')
   }
  }
  auth.onAuthStateChanged((user)=>{
  //going to controll what users see if logged in or out
  if(user){
  //console.log('user is logged in', user)
   setupUI(user)
  } else {
  //console.log('user is logged outer',user)
  setupUI()
  }
  })
   </script>
  <script src="./js/materialize.min.js"></script>
  <script src="./js/init.js" type="module"></script>
  <script src="./fireConfig.js" type="module"></script>
  </body>
</html>
